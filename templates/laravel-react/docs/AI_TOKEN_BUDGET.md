# AI Token Budget

- Read `README.md`, `AGENTS.md`, `docs/AI_CONTEXT.md`, and recent snapshots
  first.
- Search by Laravel layer before opening files.
- Avoid `vendor`, `node_modules`, storage caches, logs, secrets, and lockfiles
  unless directly relevant.
- Prefer slices over whole files.
