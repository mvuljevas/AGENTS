# AI Token Budget

- Read `README.md`, `AGENTS.md`, `docs/AI_CONTEXT.md`, and recent snapshots
  first.
- Inspect `manifest.json` before content/background scripts.
- Avoid generated files, caches, secrets, and lockfiles.
- Prefer slices over whole files.
