# AI Token Budget

- Read `README.md`, `AGENTS.md`, `docs/AI_CONTEXT.md`, and recent snapshots
  first.
- Search `src/` before opening files.
- Avoid `node_modules`, `dist`, lockfiles, caches, and generated output.
- Prefer file slices over full files.
